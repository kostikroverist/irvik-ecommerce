import { InputAutoWidthDirective } from './input-auto-width.directive';

describe('InputAutoWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAutoWidthDirective();
    expect(directive).toBeTruthy();
  });
});
